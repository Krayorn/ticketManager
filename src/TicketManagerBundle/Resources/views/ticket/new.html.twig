{% extends 'TicketManagerBundle::layout.html.twig' %}

{% block navLinks %}
    <li><a href="{{ path('ticket_index') }}">Back to the list</a></li>
    {{ parent() }}
{% endblock %}

{% block content %}
    <h1>Ticket creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="red accent-2 col s2 offset-s1 btn waves-effect waves-light" type="submit" name="action">
                Create
            </button>
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="red accent-2 col s2 offset-s1 btn waves-effect waves-light" href="{{ path('ticket_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
